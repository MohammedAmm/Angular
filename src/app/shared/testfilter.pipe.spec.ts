import { TestfilterPipe } from './testfilter.pipe';

describe('TestfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TestfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
